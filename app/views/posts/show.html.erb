<% provide(:title, @post.title) %>

<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<h2><%= @post.title %></h2>
		<% @tags.each do |tag| %>
			<%= link_to tag, user_tag_path(@user,tag) %>
		<% end %>
		<%= simple_format(@post.content) %>
	</div>
</div>